import React from 'react';
import styles from './styles.less'

export const Header = () => {
    return (
        <div className={styles.header}>
            <p className={styles.headerText}>Scroll Indicator</p>
        </div>
    );
};
